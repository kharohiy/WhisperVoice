<Window x:Class="WhisperVoice.NotepadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource WindowTitleNotepad}" Height="350" Width="500"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Closing="Window_Closing" Background="#F9F9F9">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TxtResult" Grid.Row="0" TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" FontSize="15" FontFamily="Consolas" 
                 BorderBrush="#CCCCCC" BorderThickness="1" Padding="8"/>

        <Separator Grid.Row="1" Margin="0,15,0,15" Background="#E0E0E0"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{DynamicResource BtnCopyAllText}" Style="{StaticResource BtnPrimary}" HorizontalAlignment="Stretch" Click="BtnCopy_Click"/>
            <Button Grid.Column="2" Content="{DynamicResource BtnClearAllText}" Style="{StaticResource BtnDefault}" HorizontalAlignment="Stretch" Click="BtnClear_Click"/>
        </Grid>
    </Grid>
</Window>